# =============================================================================
# NBP Slides Web - 环境变量配置模板
# =============================================================================
# 复制此文件为 .env.local 并填入实际的配置值

# -----------------------------------------------------------------------------
# Cloudflare R2 存储配置（用于文件上传）
# -----------------------------------------------------------------------------
# 从 Cloudflare Dashboard → R2 → Manage R2 API tokens 获取
R2_ACCOUNT_ID=your_account_id_here
R2_ACCESS_KEY_ID=your_access_key_here  
R2_SECRET_ACCESS_KEY=your_secret_key_here
R2_BUCKET_NAME=your_bucket_name_here

# R2 公开访问URL（可选，如果没有设置会使用默认URL）
# 格式: https://your-custom-domain.com 或 https://your-bucket.your-account-id.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://your-custom-domain.com

# -----------------------------------------------------------------------------
# Python 后端服务配置
# -----------------------------------------------------------------------------
# Railway或其他部署平台的API地址
API_URL=https://your-backend-service.railway.app
# 或者本地开发时使用
# API_URL=http://localhost:8000

# 用于客户端的公开API地址（如果前端需要直接访问）
NEXT_PUBLIC_API_URL=https://your-backend-service.railway.app

# -----------------------------------------------------------------------------
# API 行为配置（可选）
# -----------------------------------------------------------------------------
# 强制所有API都使用后端代理（默认false）
FORCE_BACKEND_PROXY=false

# 启用API自动切换（默认true）
ENABLE_AUTO_SWITCH=true

# 默认图片参数
DEFAULT_ASPECT_RATIO=16:9
DEFAULT_IMAGE_SIZE=2K

# API调用超时时间（毫秒）
API_TIMEOUT=120000

# -----------------------------------------------------------------------------
# Next.js 配置
# -----------------------------------------------------------------------------
# 生产环境使用的基础URL
NEXTAUTH_URL=https://your-domain.com

# 开发环境
NODE_ENV=development